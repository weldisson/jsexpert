const { describe, before, beforeEach, afterEach, it } = require('mocha');
const { expect } = require('chai');

const BaseRepository = require('./../../src/repository/base/BaseRepository');

describe('BaseRepository Suite Tests', () => {
    
    it('should return a content when called #find method without itemId on params', () => {

    })
})